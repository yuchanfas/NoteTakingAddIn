<UserControl x:Class="NoteTakingAddIn.KnowledgeMapArea"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="2000" d:DesignWidth="2000">
    <!-- Maniputationのイベントハンドラ名|| Canvasの名前 || Arrowを使用することはここで設定している -->
    <Canvas Background="Wheat"
          MouseLeftButtonDown="Window_MouseLeftBUttonDown"
          MouseLeftButtonUp="Window_MouseLeftButtonUp"
          MouseMove="Window_MouseMove"
          x:Name="grid1"
          xmlns:ctrl="clr-namespace:NoteTakingAddIn"
          IsManipulationEnabled="True"
          >
        <!--
        Canvasをドラッグすると、Canvas表示領域をドラッグした方向に移動できる
        
        <Canvas.RenderTransform>
            <MatrixTransform x:Name="canvasMatrixTransform"/>
        </Canvas.RenderTransform>
        -->
    </Canvas>
</UserControl>
